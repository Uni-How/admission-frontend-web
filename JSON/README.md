# JSON 資料規範統一規則

**版本**: v1.0  
**適用學年度**: 114  
**更新日期**: 2025-12-04

---

## 📋 必填欄位清單

### School 層級
```
school_id          [string]  學校代碼
school_name        [string]  學校名稱
school_type        [string]  學校類型（國立/私立/公立）
school_images      [array]   圖片URL（可空陣列）
school_url         [string]  官網
campuses           [array]   校區列表（至少1個）
departments        [array]   系所列表
```

### Campus 層級
```
campus_id          [string]  校區代碼
campus_name        [string]  校區名稱
is_main            [boolean] 是否主校區（至少1個true）
location.city      [string]  城市
location.district  [string]  行政區
location.address   [string]  地址
```

### Department 層級
```
department_id      [string]  系所代碼（全國唯一）
department_name    [string]  系所名稱
college            [string]  學院（不可空字串）
academic_group     [string]  學群分類
campus_ids         [array]   所屬校區ID
years_of_study     [number]  修業年限
admission_data     [object]  招生資料
```

### Admission Plan 層級
```
quota              [number]  招生名額（數字非字串）
exam_thresholds    [array]   檢定標準（可空陣列）
```

---

## 🎯 學群分類標準

```
資訊, 工程, 數理化, 醫藥衛生, 生命科學, 生物資源,
地球環境, 建築設計, 藝術, 社會心理, 大眾傳播,
外語, 文史哲, 教育, 法政, 管理, 財經, 遊憩運動, 不分系
```

---

## ⚙️ 型別規則

### ✅ 正確
```json
"quota": 80                    // 數字
"college": "電機資訊學院"       // 有意義的字串
"exam_thresholds": []          // 空陣列（無檢定）
"selection_multipliers": [...]  // 陣列
"multiplier": null             // null（無倍率限制）
```

### ❌ 錯誤
```json
"quota": "80"                  // 字串（應為數字）
"college": ""                  // 空字串（應有內容）
"exam_thresholds": null        // null（應為陣列）
"selection_multipliers": ""    // 字串（應為陣列）
```

---

## 📐 空值處理規則

| 欄位類型 | 必填 | 選填 |
|---------|------|------|
| **字串** | 不可空字串 `""` | 用 `null` 或省略 |
| **數字** | 不可 `null` | 用 `null` 或省略 |
| **陣列** | 可空陣列 `[]` | 用 `[]` 或省略 |
| **物件** | 不可 `null` | 用 `null` 或省略 |

---

## 🚫 資料缺失處理規範

### 原則
**爬蟲無法取得資料時，依照欄位性質填入對應的預設值**

### 必填欄位缺失處理

| 欄位類型 | 無法取得時填入 | 範例 |
|---------|--------------|------|
| **學校/系所名稱** | `"未知"` 或 `"資料缺失"` | `"school_name": "未知"` |
| **學校類型** | `"未分類"` | `"school_type": "未分類"` |
| **學院** | `"未分類學院"` | `"college": "未分類學院"` |
| **學群** | `"不分系"` | `"academic_group": "不分系"` |
| **城市/地址** | `"未知"` | `"city": "未知"` |
| **招生名額** | `0` | `"quota": 0` |
| **修業年限** | `4` | `"years_of_study": 4` |
| **陣列** | 空陣列 `[]` | `"exam_thresholds": []` |

### 選填欄位缺失處理

| 欄位類型 | 無法取得時填入 | 範例 |
|---------|--------------|------|
| **圖片URL** | 空陣列 `[]` | `"school_images": []` |
| **網址** | `null` | `"school_url": null` |
| **去年資料** | 整個物件省略 | 不寫 `last_year_pass_data` |
| **英聽門檻** | `null` | `"english_listening_threshold": null` |
| **術科類別** | `null` | `"art_test_category": null` |
| **備註說明** | `null` | `"note": null` |

### 特殊情況處理

#### 1. 整個招生管道無資料
```json
// ❌ 錯誤：放空物件
"plans": {
  "personal_application": {}
}

// ✅ 正確：省略整個管道
"plans": {
  "distribution_admission": { ... }
  // personal_application 整個省略
}
```

#### 2. 學年度無任何招生資料
```json
// ✅ 正確：標註無資料並省略
"admission_data": {
  "113": { ... },
  // 114 整個省略（表示本年度無招生）
}
```

#### 3. 檢定標準部分科目缺失
```json
// ✅ 正確：只列出有的科目
"exam_thresholds": [
  { "subject": "英文", "exam_type": "學測", "threshold": "均標" }
  // 其他科目無檢定就不列出
]

// ✅ 或：完全無檢定
"exam_thresholds": []
```

#### 4. 倍率資料不完整
```json
// ❌ 錯誤：用 0 或空字串
{ "subject": "數學A", "multiplier": 0, "order": 1 }

// ✅ 正確：用 null 表示無倍率限制
{ "subject": "數學A", "multiplier": null, "order": 1 }

// ✅ 或：整個陣列為空
"selection_multipliers": []
```

#### 5. 去年通過資料不完整
```json
// 🟡 有部分資料：填入已知的
"last_year_pass_data": {
  "academic_year": 113,
  "passing_sequence": [
    { "subject": "總級分", "grade": 50, "note": null }
  ]
}

// ✅ 完全無資料：整個物件省略
// 不寫 last_year_pass_data
```

### 驗證工具處理

- **空陣列 `[]`**: 驗證通過（表示無資料）
- **`null`**: 選填欄位通過，必填欄位警告
- **`"未知"`/`"未分類"`**: 通過但會產生警告
- **`0`**: 數字欄位通過（表示該年度不招生）

---

## 🔢 招生管道結構

### 個人申請 (personal_application)
```json
{
  "quota": 80,                           // [必填][number]
  "exam_thresholds": [...],              // [必填][array] 學測檢定
  "selection_multipliers": [...],        // [必填][array] 篩選倍率
  "english_listening_threshold": "B級",  // [選填][string|null]
  "last_year_pass_data": {              // [選填][object]
    "academic_year": 113,
    "passing_sequence": [
      { "subject": "國英數A自", "grade": 55, "note": "四科組合" }
    ]
  }
}
```

### 分發入學 (distribution_admission)
```json
{
  "quota": 45,                           // [必填][number]
  "exam_thresholds": [...],              // [必填][array]
  "english_listening_threshold": null,   // [選填][string|null]
  "scoring_weights": [...],              // [必填][array] 採計加權
  "art_test_category": null,             // [選填][string|null]
  "tie_breakers": [...],                 // [選填][array]
  "last_year_pass_data": {...}          // [選填][object]
}
```

### 繁星推薦 (star_plan)
```json
{
  "quota": 10,                           // [必填][number]
  "exam_thresholds": [...],              // [必填][array]
  "ranking_criteria": [...],             // [必填][array]
  "last_year_pass_data": {...}          // [選填][object]
}
```

---

## 📊 子結構定義

### ExamThreshold（檢定標準）
```json
{
  "subject": "國文",        // 科目名稱
  "exam_type": "學測",      // 考試類型
  "threshold": "均標"        // 檢定標準（頂/前/均/後/底）
}
```

### SelectionMultiplier（篩選倍率）
```json
{
  "subject": "數學A",        // 科目名稱
  "multiplier": 5,           // 倍率（null=僅比序）
  "order": 1                 // 篩選順序
}
```

### ScoringWeight（採計加權）
```json
{
  "subject": "數學甲",       // 科目名稱
  "source_type": "分科",     // ★必填：學測/分科
  "multiplier": 2.0,         // 加權倍數
  "order": 1                 // 同分參酌順序
}
```

### PassingSequence（通過資料）
```json
{
  "subject": "國英數A自",    // 科目或類別
  "grade": 55,               // 通過級分/分數
  "note": "四科組合"         // 備註
}
```

---

## 🌐 全局配置

### 學測級分標準
**位置**: `JSON/configs/assessment_standards_114.json`

**說明**: 全國統一標準，不在各系所重複

**內容**: 
- 6科五標（國/英/數A/數B/社/自）
- 英聽級別（A/B/C/F）

---

## ✅ 驗證檢查點

執行驗證工具：
```bash
npx tsx scripts/validate_crawler_data.ts JSON/your_data.json
```

### 常見錯誤
1. ⛔ `quota` 是字串 → 改為數字
2. ⛔ `selection_multipliers` 是字串 → 改為陣列
3. ⛔ `college` 為空 → 填入學院名稱
4. ⛔ `scoring_weights` 缺 `source_type` → 補上
5. ⛔ 必填陣列用 null → 改為 `[]`

---

## 📁 檔案結構

```
JSON/
├── configs/
│   └── assessment_standards_114.json   # 學測級分標準
├── standard.json                        # 完整規格說明
└── school_data_*.json                   # 實際學校資料
```

---

## 🔄 更新歷史

### v1.0 (2025-12-04)
- 新增 `academic_group` 欄位
- 新增 `scoring_weights.order` 欄位
- 分離 `assessment_standards` 為全局配置
- 明確空值處理規則
- 確認 `last_year_pass_data` 保留詳細結構

---

**維護**: 每次標準變更須同步更新此文檔  
**驗證**: 所有爬蟲資料必須通過驗證工具檢查
